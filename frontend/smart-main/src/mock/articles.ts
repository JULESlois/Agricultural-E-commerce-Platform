import { Article, Expert } from '../../types';
import { MOCK_ARTICLES, MOCK_EXPERTS } from '../../constants';

export const experts: Expert[] = [...MOCK_EXPERTS];
export const articles: Article[] = [...MOCK_ARTICLES];
