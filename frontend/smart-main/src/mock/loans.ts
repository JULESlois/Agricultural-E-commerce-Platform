import { LoanProduct, LoanApplication } from '../../types';
import { MOCK_LOANS, MOCK_APPLICATIONS } from '../../constants';

export const loans: LoanProduct[] = [...MOCK_LOANS];
export const applications: LoanApplication[] = [...MOCK_APPLICATIONS];
